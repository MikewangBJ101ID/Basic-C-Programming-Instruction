#include <stdio.h> # Notify the Compiler to use default value and standard C I/O function
printf("  ");
scanf("");

/* format specifier*/
/* %c(int or chat as ASCII Format), %d, %e, %f, %% */
/* %u(unsigned int as decimal), %x, %X */



# Both Printf and Scanf have return value
